# 2026-07-14 工作日志

## 任务：手机端静态菜谱网站（老乡鸡 / CookLikeHOC）

### 完成内容
- 克隆开源菜谱仓库 `Gar-b-age/CookLikeHOC` 到 `cooklikehoc-src/`（溯源用，保留）
- `build/parse-recipes.mjs` 解析 333 道菜 → `data/recipes.json` + `data/recipes.js`（window.RECIPES，避免 file:// CORS）
- 15 个分类；字段：id/name/category/imagePath/ingredientCount/stepCount/ingredients[]/steps[]
- 用 HEYTEA 无字版涂鸦风（真实菜为锚点 + 黑线小人 + 无文字 + 留白）AI 生成 12 道招牌菜配图
- 后台代理生成了约 222 张后取消 → 清理菜名（markdown 链接/斜杠）→ 回收 gen_tmp 未移出图 → 最终 231 道关联配图
- 手写前端：`index.html` / `styles.css` / `app.js`（原生 HTML/CSS/JS，无框架）
- **风格改版（第三阶段）**：纸本涂鸦风（墨黑 #1F1A16 主色 + 暖纸 #F2EBDE 底 + Ma Shan Zheng 品牌字体 + 直角粗黑边框 + 3:4 竖版展示 + 菱形类别标签）
- 修复：`body { opacity: 0; }` 致透明页 bug、菜名 markdown 语法/slash 清洗、旧赤陶色统一为墨黑

### 关键约束 / 教训
- ImageGen 的 output_dir 必须用反斜杠 Windows 路径，否则被忽略
- 后台 agent（run_in_background）在本环境不可用（TaskStop 缺失）→ 主线程直调更稳妥

### 关键约束 / 教训
- ImageGen 并行调用会文件名碰撞 → 改为顺序单张生成
- AI 图带"图片由AI生成"水印 → Pillow 裁底部 90px 去除（`build/crop-heroes.py`）
- 沙箱 loopback 被限制，无法直接 curl 本地预览；用 present_files 打开 HTML 走内置预览面板
- 模型无法目视图片，配图质量靠提示词约束 + 工具成功状态判断，需用户最终目视校验

### 验证
- `node --check app.js` 通过
- recipes.js：333 菜 / 15 分类 / 12 带图，图片路径全部命中无缺失
- 已清理 `assets/images/gen`、`gen_first` 冗余中间产物

### Git 部署
- 2026-07-14 16:44: 推送 333 道菜 + 231 张配图到 GitHub `cyhzzz/caipu-recipes`
- GitHub Pages 上线：**https://cyhzzz.github.io/caipu-recipes/**
- SSH 推送（PAT 用于 API 创建仓库）；811 个文件
